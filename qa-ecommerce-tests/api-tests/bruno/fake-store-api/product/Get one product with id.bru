meta {
  name: Get one product with id
  type: http
  seq: 2
}

get {
  url: {{base_URL}}/products/{{productId}}
  body: none
  auth: inherit
}

script:pre-request {
  
  
}

tests {
  test('Status code is 200', () => {
    expect(res.status).to.equal(200);
  });
  
  test('Product id matches expected id', () => {
    expect(res.body.id).to.equal(1);
  });
  
  
  
  
}

settings {
  encodeUrl: true
  timeout: 0
}
